import React from 'react';
import Layout from '../../components/layout';

function index(props) {
    return (
        <Layout>
            <h1>Payment</h1>
        </Layout>
    );
}

export default index;