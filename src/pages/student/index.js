import React from 'react';
import Layout from '../../components/layout';

function index(props) {
    return (
        <Layout>
            <h1>Students</h1>
        </Layout>
    );
}

export default index;